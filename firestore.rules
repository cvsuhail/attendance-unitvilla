rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Matches ALL documents in the database
    match /{document=**} {
      allow read, write: if true; // <-- add some rule here, required
    }

  }
}
